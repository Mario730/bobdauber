backend:
  name: git-gateway
  branch: main

media_folder: 'static/images/uploads'

site_url: 'https://bobdauber.com'

collections:
  - name: 'posts'
    label: 'Posts'
    label_singular: 'Post'
    folder: 'static/posts'
    media_folder: 'images'
    slug: "{{year}}-{{month}}-{{day}}_{{slug}}"
    create: true
    fields:
      - label: "Title"
        name: "title"
        widget: "string"
      - label: "Material"
        name: "material"
        widget: "string"
      - label: "Images"
        name: "images"
        widget: "image"
        allow_multiple: true